# 금융 온톨로지 서비스 — 프로젝트 스펙

작성일: 2026-08-03

---

## 1. 한 줄 요약

주식·코인·원자재 같은 자산들을 팔란티어 온톨로지처럼 **객체와 관계의 그래프**로 표현하고, 각 객체마다 뉴스·기업정보·AI 전망을 붙여서 탐색할 수 있게 만드는 웹 서비스.

## 2. 사용자 경험 (핵심)

화면 가운데 그래프가 있고, 노드를 클릭하면 연결된 것들이 펼쳐진다. 노드를 선택하면 오른쪽 패널에 상세 정보가 뜬다. 상단 검색창에 종목명을 입력하면 해당 노드로 바로 점프하면서 주변 관계가 자동으로 펼쳐진다.

**중요한 설계 원칙: 한 번에 다 보여주지 않는다.**
노드를 전부 뿌리면 선이 수백 개 얽혀서 "털뭉치(hairball)"가 된다. 처음엔 큰 덩어리(섹터·개념)만 보여주고, 클릭할 때마다 한 단계씩 펼치는 방식으로 간다.

- 기본 표시: 시장 루트 + 섹터 노드 + 주요 개념 노드
- 섹터 클릭 → 그 안의 종목들 펼침
- 종목 클릭 → 오른쪽 상세 패널 표시 + 직접 연결된 노드만 강조
- 검색 → 해당 노드로 점프 + 소속 섹터 자동 펼침

---

## 3. 온톨로지 스키마

### 3.1 객체 (노드)

| 타입 | 설명 | 주요 속성 |
|---|---|---|
| `Asset` | 주식 종목, 코인, 원자재 등 거래 가능한 자산 | 티커, 이름, 자산군, 시가총액, 현재가, 등락률 |
| `Company` | 종목의 발행 기업 | 기업명, 사업 개요, 주요 제품, CEO, 설립연도 |
| `Industry` | 세부 산업 (반도체, 완성차 등) | 이름, 설명 |
| `Sector` | 대분류 (기술, 에너지, 금융 등) | 이름, 설명 |
| `Concept` | **핵심 아이디어.** 금리, 인플레이션, 양자컴퓨팅, 지정학 리스크, AI 수요, 환율 등 자산이 아닌 거시 개념/테마 | 이름, 설명, 카테고리(거시/기술/정책/지정학) |
| `NewsEvent` | 개별 뉴스 기사 | 제목, 요약, 원문 URL, 발행일, 출처, 감성(긍정/중립/부정) |
| `Outlook` | Claude가 생성한 전망 텍스트 | 요약, 리스크 요인, 근거 뉴스 ID들, 생성 시각 |

**`Concept` 노드가 이 프로젝트의 차별점이다.** 자산군이 서로 달라도(주식 ↔ 코인 ↔ 금) 개념 노드를 경유해서 연결된다. "양자컴퓨팅 → 블록체인 위협", "금리 → 증시 하락" 같은 관계가 여기서 나온다.

### 3.2 관계 (엣지)

| 관계 | 방향 | 예시 | 데이터 확보 난이도 |
|---|---|---|---|
| `BELONGS_TO` | Asset → Industry → Sector | 삼성전자 → 반도체 → 기술 | 쉬움 (API 제공) |
| `COMPETES_WITH` | Asset ↔ Asset | 현대차 ↔ 기아 | 보통 (동일 산업 + 시총 기준 자동 생성 가능) |
| `AFFECTS` | Concept → Asset/Concept | 금리 → 증시, 양자컴퓨팅 → 블록체인 | Claude로 생성 |
| `SUPPLIES_TO` | Company → Company | 삼성전자 → 애플 | **어려움** (무료 데이터 거의 없음) |
| `MENTIONED_IN` | Asset → NewsEvent | — | 쉬움 |
| `HAS_OUTLOOK` | Asset → Outlook | — | 자체 생성 |

### 3.3 관계에 붙는 속성 (중요)

선을 단순한 선으로 두지 않고 세 가지 정보를 붙인다. 이게 있어야 "금리 노드 클릭 → 부정적 영향받는 종목 하이라이트" 같은 기능이 가능해진다.

- **direction**: 방향 (금리 → 증시. 반대 방향은 성립하지 않음)
- **polarity**: 부호. `positive` / `negative` / `mixed` (금리↑ → 증시↓ 이면 negative)
- **strength**: 영향 강도 1~5
- **confidence**: 이 관계의 신뢰도 0~1 (AI 생성 관계는 낮게 시작)
- **rationale**: 왜 이런 관계인지 한 줄 설명 (사용자에게 보여줄 용도)

### 3.4 노드 시각화 (아이콘·색상)

그래프 캔버스에서 노드는 "색이 채워진 원 + 흰색 아이콘" 배지 형태로 그린다. 타입별 배경색은 이미 `globals.css`의 `--node-sector` / `--node-industry` / `--node-asset` / `--node-concept` 변수로 정의돼 있고, 이번 결정은 그 위에 그릴 아이콘만 추가한다 — 색상 체계는 바꾸지 않는다.

**아이콘 폰트: Google Material Symbols Outlined.** 리거처(ligature) 방식 폰트라 `bolt`, `percent` 같은 영문 텍스트를 그대로 그리면 폰트가 그 텍스트를 아이콘 글리프로 치환해서 보여준다.

**Sector / Industry / Concept**: 노드 ID별로 아이콘을 지정하고, 매핑에 없는 새 노드는 타입별 기본 아이콘으로 대체한다. 1단계 계획(6장)에서 종목 50개·개념 노드 10개로 늘어나므로 전부 하드코딩하지 않고 이 fallback 구조를 유지한다.

| 노드 ID | 이름 | 아이콘(ligature) |
|---|---|---|
| `sector-tech` | 기술 | `memory` |
| `sector-energy` | 에너지 | `bolt` |
| `industry-semi` | 반도체 | `developer_board` |
| `industry-software` | 소프트웨어 | `code` |
| `industry-oilgas` | 석유·가스 | `oil_barrel` |
| `concept-rates` | 금리 | `percent` |
| `concept-inflation` | 인플레이션 | `trending_up` |
| `concept-quantum` | 양자컴퓨팅 | `science` |
| `concept-ai-demand` | AI 수요 | `smart_toy` |

**타입별 기본 아이콘** (매핑에 없는 노드용):

| 타입 | 기본 아이콘 |
|---|---|
| `Sector` | `category` |
| `Industry` | `factory` |
| `Concept` | `lightbulb` |

**Asset(종목)**: 아이콘 대신 티커 앞 두 글자(굵게, 흰색)를 원 안에 그린다 — 첫 글자만 쓰면 NVDA·NFLX처럼 같은 글자로 시작하는 티커가 구분이 안 돼서 두 글자로 한다. **지금은 임시 표시**다. 나중에(이번 작업 범위 아님, Finnhub 연동 시점) Finnhub `profile2` 엔드포인트의 `logo` 필드로 받아오는 실제 기업 로고 이미지로 교체하고, 렌더링마다 다시 요청하지 않도록 캐싱한다.

**상세 패널(4장)은 이번 변경 범위 밖이다.** 그래프 캔버스 노드 표시만 바꾼다.

---

## 4. 상세 패널에 들어갈 내용

종목 노드를 선택했을 때 오른쪽 패널 구성 (위에서부터 순서대로):

1. **헤더** — 종목명, 티커, 섹터/산업
2. **주가 정보** — 현재가, 등락률, 최근 1개월 미니 차트
3. **기업 개요** — 무슨 회사인지 2~3줄, 주요 사업/제품, 시가총액, CEO
4. **AI 전망 요약** — Claude가 최근 뉴스를 종합해 쓴 전망 + 주요 리스크 요인 (생성 시각 표기)
5. **최근 뉴스** — 3~5건, 제목 + 한 줄 요약 + 원문 링크 + 감성 배지
6. **연결된 관계** — 경쟁사, 영향 주는 개념 노드들 (클릭하면 그래프에서 해당 노드로 이동)

**표기 원칙:** AI가 생성한 전망에는 반드시 "AI 생성" 표시와 생성 시각을 붙이고, 투자 조언이 아니라는 안내 문구를 둔다.

---

## 5. 아키텍처

### 5.1 구성

```
[사용자 브라우저]
      ↓
[Next.js 프론트엔드]  ← Vercel 배포
  · react-force-graph (그래프 시각화)
  · 검색 / 상세 패널 UI
      ↓
[Supabase (PostgreSQL)]
  · nodes 테이블 / edges 테이블
  · news, outlooks 테이블
      ↑
[데이터 수집 파이프라인]  ← 정기 실행 (하루 1회)
  · 금융 API에서 종목·뉴스 수집
  · Claude API로 요약 및 전망 생성
```

### 5.2 기술 선택과 이유

**DB — Supabase (PostgreSQL)**
Neo4j 같은 전용 그래프 DB가 온톨로지에 더 어울려 보이지만, MVP 규모(노드 수백 개, 엣지 수천 개)에서는 Postgres의 `nodes` / `edges` 두 테이블로 충분히 빠르다. Supabase는 DB·인증·API·호스팅을 한 번에 제공하고 무료 티어가 넉넉해서 초보자에게 훨씬 다루기 쉽다. 규모가 커지면 그래프 DB로 이전을 검토한다.

**프론트 — Next.js + react-force-graph**
그래프 시각화 라이브러리 중 학습 곡선이 완만하고 문서가 잘 되어 있다. Vercel 무료 배포와 궁합이 좋다.

**데이터 — Finnhub + Claude 요약**
금융 API는 **Finnhub**을 채택한다. Financial Modeling Prep과 비교한 결과:
- Finnhub 무료 티어는 **분당 60회** (일일 한도 없음), FMP는 **하루 250회 + 최근 30일 500MB 대역폭 제한**. 종목 50개 기준 하루 파이프라인 호출량(약 100회 안팎)에는 둘 다 충분하지만, 초기 데이터 백필처럼 한 번에 몰아 부르는 상황에는 분당 제한 방식(Finnhub)이 더 유리하다.
- 이 프로젝트에 필요한 세 가지(시세, 뉴스, 회사 로고)가 Finnhub 무료 티어에 전부 포함된다 — 로고는 `stock/profile2` 엔드포인트의 `logo` 필드로 제공된다 (3.4절 참고).
- FMP도 로고 API가 있지만 "Legacy(구버전)" 표시가 붙어 있어, 계속 무료로 유지될지 확실치 않았다.

Claude API가 뉴스 요약과 전망을 생성한다. 직접 크롤링은 저작권/ToS 문제와 유지보수 부담 때문에 채택하지 않는다.

### 5.3 성능 — "느려지지 않게" 하는 핵심

**모든 AI 생성 결과는 미리 만들어 DB에 저장한다 (precompute).**

"금리 ↑ → 증시 ↓" 같은 개념 관계는 매일 바뀌지 않는다. 사용자가 클릭할 때마다 AI에게 묻는 게 아니라, 파이프라인이 하루 1회 미리 생성해서 DB에 넣어둔다. 사용자가 보는 시점에는 저장된 값을 읽기만 하므로 즉시 표시된다.

추가 원칙:

- 그래프는 기본적으로 **1-hop만 조회** (선택 노드에 직접 연결된 것만). 전체 그래프를 한 번에 로드하지 않는다.
- 뉴스는 종목당 최근 5건까지만 저장/표시
- `edges` 테이블에 `source_id`, `target_id` 인덱스 필수
- 상세 패널 데이터는 클라이언트에서 캐시

---

## 6. 단계별 계획

한국+미국 주식 전체에 관계 4종류와 개념 노드까지 한 번에 만들면 첫 프로젝트로는 너무 크다. 아래 순서로 나눈다.

### 1단계 — 작동하는 최소 버전 (여기에 집중)

- **범위**: 미국 주식 50개 (S&P 500 중 대표 종목), 개념 노드 10개
- **관계**: `BELONGS_TO`, `COMPETES_WITH`, `AFFECTS`
- **기능**: 그래프 표시 / 클릭 확장 / 검색 점프 / 상세 패널
- **데이터**: 금융 API로 종목·뉴스 수집, Claude로 요약·전망·개념 관계 생성

이 단계가 돌아가는 걸 확인하는 게 최우선. 나머지는 여기서 확장하면 훨씬 쉬워진다.

### 2단계 — 확장

- 한국 주식 추가 (뉴스 한국어 처리)
- 코인 / 금 / 석유 등 자산군 추가
- 원자재 영향 관계 (`AFFECTS`의 확장)
- 개념 노드 30개 수준으로 확대

### 3단계 — 고급

- 공급망 관계 (`SUPPLIES_TO`) — 데이터 확보 방법을 먼저 검증할 것
- 관계 경로 탐색 ("금값이 오르면 어떤 종목이 영향받나" 다단계 추론)
- 사용자 계정 / 관심 종목 저장

---

## 7. 구현 순서 (실제 작업 흐름)

1. Next.js 프로젝트 생성 + Vercel 배포 확인 (빈 페이지라도 일단 배포)
2. react-force-graph로 하드코딩된 노드 10개 그려보기
3. 클릭 확장 + 상세 패널 UI 구현 (데이터는 아직 가짜)
4. Supabase 프로젝트 생성 + `nodes` / `edges` 테이블 설계
5. 종목 50개 + 섹터/산업 데이터 수동 또는 API로 입력
6. 프론트를 Supabase에 연결 (가짜 데이터 → 실제 데이터)
7. 금융 API 연동해서 주가·뉴스 수집 스크립트 작성
8. Claude API로 뉴스 요약 + 전망 생성
9. 개념 노드와 `AFFECTS` 관계를 Claude로 생성해서 DB에 저장
10. 검색 기능 추가
11. 파이프라인 정기 실행 설정 (하루 1회)

**핵심: 3번까지는 데이터 없이 화면만으로 진행한다.** 눈에 보이는 게 먼저 나와야 이후 작업이 수월하다.

---

## 8. 미해결 / 나중에 결정할 것

- 한국 주식 데이터 소스 (무료 옵션이 제한적)
- 개념 노드 초기 목록 확정 (10개를 무엇으로 할지)
- 공급망 데이터 확보 방법
- 뉴스 원문 저장 범위 (저작권 고려 — 제목·요약·링크만 저장하고 전문은 저장하지 않는 방향)
- 종목 로고 이미지 연동 (Finnhub `profile2.logo`) — Asset 노드의 티커 앞 두 글자 임시 표시를 실제 로고로 교체, 캐싱 전략 포함 (3.4절 참고)
- AI가 스스로 매기는 confidence(신뢰도) 값의 한계 — 모델 자체 판단이라 실제 신뢰도와 차이가 날 수 있음. 나중에 사람 검토·백테스트 기반 보정 방법 검토 필요.

---

## 9. 주의사항

- **투자 조언이 아님.** AI 생성 전망은 참고 정보일 뿐이며, 서비스 전반에 이 점을 명시한다.
- **뉴스 저작권.** 원문 전체를 복제해 저장·표시하지 않는다. 제목·짧은 요약·원문 링크까지만.
- **AI 생성물 표시.** 전망과 개념 관계는 AI가 만든 것임을 사용자가 알 수 있게 표기하고, `confidence` 값을 함께 노출하는 것을 검토한다.
- **API 무료 한도.** 종목 수를 늘릴 때 호출 횟수가 한도를 넘지 않는지 확인한다.

---

## 10. 개발 환경

- 에디터: Visual Studio Code
- AI 코딩: Claude Code (VS Code 확장 — Extensions 패널에서 "Claude Code" 검색해 설치, CLI 번들 포함)
- 실행: VS Code 통합 터미널에서 `claude` 명령
- 이 문서를 프로젝트 루트에 두고 Claude Code가 참조하도록 한다
