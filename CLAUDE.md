# 프로젝트 안내 (Claude Code용)

## 이 프로젝트가 뭔지

금융 자산(주식·코인·원자재)을 팔란티어 온톨로지처럼 **객체와 관계의 그래프**로 표현하는 웹 서비스.
전체 스펙은 `금융-온톨로지-스펙.md`에 있다. **작업 시작 전 반드시 이 문서를 먼저 읽을 것.**

## 사용자에 대해

- 코딩 경험이 많지 않다. 바이브 코딩 방식으로 진행한다.
- 전문 용어를 쓸 때는 짧게 풀어서 설명한다. 설명 없이 약어나 라이브러리명을 던지지 않는다.
- 한국어로 대화한다.
- **모르는 건 모른다고 말한다.** 추측을 사실처럼 쓰지 않는다.

## 기술 스택

- 프론트: Next.js + react-force-graph
- DB/백엔드: Supabase (PostgreSQL)
- 배포: Vercel
- 데이터: 금융 API (Finnhub 또는 Financial Modeling Prep) + Claude API 요약

## 현재 단계

**1단계 진행 중** — 미국 주식 50개 + 개념 노드 10개로 작동하는 최소 버전 만들기.
2단계(한국 주식·코인·원자재)와 3단계(공급망)는 아직 착수하지 않는다. 범위를 임의로 넓히지 말 것.

## 설계 원칙 (지켜야 함)

1. **한 번에 다 보여주지 않는다.** 그래프는 단계적으로 펼친다. 전체 노드를 한 번에 렌더링하면 털뭉치가 된다.
2. **AI 생성 결과는 미리 만들어 DB에 저장한다.** 사용자 클릭 시점에 Claude API를 호출하지 않는다. 파이프라인이 하루 1회 생성해 저장하고, 런타임에는 읽기만 한다.
3. **그래프 조회는 기본 1-hop.** 선택 노드에 직접 연결된 것만 가져온다.
4. **`Concept` 노드가 이 프로젝트의 핵심.** 금리·양자컴퓨팅 같은 개념 노드를 통해 서로 다른 자산군이 연결된다. 단순 종목-섹터 그래프로 축소하지 말 것.
5. **엣지에는 방향·부호·강도·신뢰도·근거를 붙인다.** 관계를 단순 연결선으로 만들지 않는다.

## 작업 순서

`금융-온톨로지-스펙.md`의 7장 구현 순서를 따른다.
특히 **3번 항목까지는 실제 데이터 없이 화면만으로 진행한다.** 눈에 보이는 결과가 먼저 나와야 한다.

## 하지 말아야 할 것

- 뉴스 원문 전체를 복제해 저장하지 않는다 (제목·짧은 요약·링크까지만).
- 투자 조언으로 읽힐 문구를 쓰지 않는다. AI 전망에는 생성 시각과 "AI 생성" 표시를 붙인다.
- 스펙에 없는 기능을 임의로 추가하지 않는다. 필요해 보이면 먼저 물어본다.
- API 키를 코드에 직접 적지 않는다. 환경변수(`.env.local`)를 쓰고 `.gitignore`에 포함한다.
